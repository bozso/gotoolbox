package encode

import (
    "encoding/base64"
)

const tpl = "data:{klass}/{mode};charset=utf-8;base64,{data}"

const tpl = "data:{klass}/{mode};charset=utf-8;base64,{data}"

type Base64 struct {
    
}
